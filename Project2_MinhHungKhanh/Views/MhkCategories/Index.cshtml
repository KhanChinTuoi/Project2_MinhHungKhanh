@model IEnumerable<Project2_MinhHungKhanh.Models.MhkCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MhkCategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MhkCategoryName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MhkCategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MhkCategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MhkCategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
