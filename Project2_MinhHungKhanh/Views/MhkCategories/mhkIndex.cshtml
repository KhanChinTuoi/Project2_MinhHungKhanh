@model IEnumerable<Project2_MinhHungKhanh.Models.MhkCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="MhkCreate">Tạo mới</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MhkCategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MhkCategoryName)
            </td>
            <td>
                    <a asp-action="MhkEdit" asp-route-id="@item.MhkCategoryId">Sửa</a> |
                    <a asp-action="MhkDetails" asp-route-id="@item.MhkCategoryId">Chi tiết</a> |
                    <a asp-action="MhkDelete" asp-route-id="@item.MhkCategoryId">Xóa</a>
            </td>
        </tr>
}
    </tbody>
</table>
